// configuração da camera
const config = {
    video: { width: 640, height: 480, fps: 30 }
}

// cores dos dedos
const landmarkColors = {
    thumb: 'red',
    index: 'blue',
    middle: 'yellow',
    ring: 'green',
    pinky: 'pink',
    wrist: 'white'
}

export {config, landmarkColors}